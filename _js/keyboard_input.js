export function initKeyboardInput() {
    window.addEventListener('keydown', keyDown);
}
function keyDown(e) {
    // console.log(`${e.key}`);
    switch (e.key) {
        case 'Enter':
            break;
        case 'a':
            break;
    }
}
//# sourceMappingURL=keyboard_input.js.map